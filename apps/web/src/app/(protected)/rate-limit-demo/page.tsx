export default function RateLimitDemoPage() {
  return (
    <section className="rounded-[2rem] border border-dashed border-neutral-300 bg-white p-8 shadow-sm">
      <p className="text-sm font-semibold uppercase tracking-[0.2em] text-neutral-500">
        Phase 5 placeholder
      </p>
      <h1 className="mt-3 text-3xl font-semibold tracking-tight text-neutral-950">
        Rate-limit demo
      </h1>
      <p className="mt-3 max-w-2xl text-sm leading-7 text-neutral-600">
        This page is intentionally reserved for the Upstash-backed rate-limit exercise in Phase 5.
        The shell and navigation are already wired so the follow-up work can drop in cleanly.
      </p>
    </section>
  );
}
