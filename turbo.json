{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalEnv": ["NODE_ENV", "CI"],
  "globalDependencies": ["vitest.shared.ts", "vitest.workspace.ts"],
  "tasks": {
    "topo": {
      "dependsOn": ["^topo"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "inputs": ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", "biome.json", "tsconfig.json"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["topo"],
      "inputs": ["**/*.ts", "**/*.tsx", "tsconfig.json"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["topo"],
      "inputs": ["$TURBO_DEFAULT$", "vitest.config.ts"],
      "env": [
        "DATABASE_URL",
        "TEST_DATABASE_URL",
        "CLERK_SECRET_KEY",
        "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY",
        "CORS_ALLOWED_ORIGINS"
      ],
      "outputs": ["coverage/**"]
    }
  }
}
